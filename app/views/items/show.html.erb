<h2><%= @item.name %></h2>
<div><%= @item.description %></div>
<div><%= @item.price %></div>

<% if @item.active? %>
  <%= button_to "Add to cart", user_carts_path(item_id: @item.id, create: true) %>
<% else %>
   <p>
     I'm Retired
   </p>
<% end  %>
