<h2><%= @item.name %></h2>
<div><%= @item.description %></div>
<div><%= @item.price %></div>

<% if @item.active? %>
  <%= button_to "Add to cart", cart_items_path(id: @item) %>
<% else %>
   <p>
     Item Retired
   </p>
<% end  %>
