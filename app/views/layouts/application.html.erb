<!DOCTYPE html>
<html>
  <head>
    <title>Hiptique</title>
<link href='https://fonts.googleapis.com/css?family=Lato' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  </head>
  <body>
      <nav class ="navbar navbar-default">
        <div class="container-fluid" id="custom-nav">
           <div class="navbar-header" id="icon">
            <%= link_to image_tag('icon.png'), items_path %>
          </div>
          <ul class="nav navbar-nav navbar-right">

          <li><%= link_to "Sign In"%></li>
          <li><%= link_to "Create Account", new_user_path, method: :get %></li>
          <li><button type="button" class="btn btn-default btn-lg">
  <span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span></button></li>
        </ul>
      </div>
      </nav>
      <div class="backgound-image">
      </div>
  </body>

  <% flash.each do |name, message| %>
    <%= content_tag :div, message, id: "flash_#{name}" %>
  <% end  %>

<%= yield %>
</div>
</html>
