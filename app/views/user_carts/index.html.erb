<h2>My Cart</h2>

<% @cart.obj_contents.each do |item, total| %>
  <div id="item-<%= item.name %>">
    <img src="<%= item.image %>"></img>
    <div><%= item.name %><div>
    <%= item.description %>
    <%= item.price %>
    <%= total %>
  </div>
<% end %>

Total: <%= @cart.total %>
